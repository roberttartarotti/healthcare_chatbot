[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "healthcare-celery"
version = "0.0.1"
description = "Celery tasks management library for healthcare chatbot system"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Robert Tartarotti", email = "robert.tartarotti@gmail.com"}
]
keywords = ["celery", "healthcare", "tasks", "chatbot", "queue"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: System :: Distributed Computing",
    "Topic :: Healthcare",
]

dependencies = [
    "celery>=5.3.0",
    "python-decouple>=3.8",
    "healthcare-mongodb>=0.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=22.0.0",
    "flake8>=5.0.0",
    "mypy>=1.0.0",
]

[project.scripts]
start_celery_worker = "healthcare_celery.worker:main"
start_celery_beat = "healthcare_celery.beat:main"

[project.urls]
Homepage = "https://github.com/healthcare-chatbot/lib_celery"
Repository = "https://github.com/healthcare-chatbot/lib_celery"
Issues = "https://github.com/healthcare-chatbot/lib_celery/issues"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.dynamic]
version = {attr = "healthcare_celery.version.__version__"}

[tool.black]
line-length = 88
target-version = ['py38']

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --cov=healthcare_celery --cov-report=term-missing" 